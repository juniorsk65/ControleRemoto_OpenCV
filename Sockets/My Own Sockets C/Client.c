#include <stdio.h>
#include <sys/socket.h>

//int main()
//{
/*    int status_connect = connect(socket_desc, )
    ///sockid:           integer, socket to be used in connection
    ///foreignAddr:      struct sockaddr: address of the passive participant
    ///addrlen:          integer, sizeof(name)
    if(status_connect == 0)
        printf("O connect ocorreu com sucesso. \n");
    else
        printf("Erro de connect. \n");





*/
//}
